{
  "name": "netlify-token-request",
  "version": "",
  "scripts": {
    "build": "tsc",
    "build:production": "npm run prestart && npm prune --production"
  },
  "description": "",
  "devDependencies": {
    "@netlify/functions": "^1.0.0",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "@types/node": "^17.0.21",
    "ably": "^1.2.17",
    "dotenv": "^16.0.0"
  }
}
